services:
  - type: web
    name: haaris-backend-1
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port 3050
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
